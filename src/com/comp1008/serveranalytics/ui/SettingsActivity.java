package com.comp1008.serveranalytics.ui;

import android.app.Activity;
import android.content.Intent;
import android.os.Bundle;
import android.util.Log;
import android.view.Menu;
import android.view.View;
import android.view.View.OnClickListener;
import android.widget.Button;
import android.widget.RadioButton;
import android.widget.RadioGroup;

import com.comp1008.serveranalytics.R;


public class SettingsActivity extends Activity {
	//IGNORE THIS NONE OF IT WORKS
/*
	 Intent intent;
	 Button doneSettingsButton;	
	 
		RadioGroup radFreq;
		RadioButton radOption1;
		RadioButton radOption2;
		RadioButton radOption3;
		RadioButton radOption4;

	protected void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.activity_settings);
		runit();
	}
	
	
	 public void runit() {
			// Creates our views, in this case 3 buttons
		 
			
			radFreq = (RadioGroup) findViewById(R.id.radio_group);
			radOption1 = (RadioButton)findViewById(R.id.radio0);
			radOption2 = (RadioButton)findViewById(R.id.radio1);
			radOption3 = (RadioButton)findViewById(R.id.radio2);
			radOption4 = (RadioButton)findViewById(R.id.radio3);
			
			
			doneSettingsButton = (Button) findViewById(R.id.doneSettingsButton);
			doneSettingsButton.setOnClickListener((OnClickListener) this); 
		    }

	 public void onClick(View v) {
			
			switch(v.getId()){
			
			case(R.id.doneSettingsButton):
			    intent = new Intent(SettingsActivity.this, MainMenuActivity.class);
			    SettingsActivity.this.startActivity(intent);
			break;
			
			default:
			    break;
			}
		    }
		
	
	 @Override
	    public boolean onCreateOptionsMenu(Menu menu) {
		// Inflate the menu; this adds items to the action bar if it is present.
		getMenuInflater().inflate(R.menu.settings_page, menu);
		return true;
	    }

	*/
}

